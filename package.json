{
  "name": "maamirkh-log-absolute",
  "version": "1.0.0",
  "description": "Calculate and log the absolute difference between the number -5 and 5.",
  "main": "index.js",
  "type": "module",
  "bin": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.12.4"
  }
}
